exclude_dirs:
  - .venv
  - build
  - dist
  - tests
tests: []
skips:
  - B101 # asserts document table invariants in tests
  - B113 # retained for compatibility with tools that may inspect requests-like patterns
